# CLAUDE.md

웹으로 작성하고 볼 수 있는 **업무용 달력** 프로젝트. 이 문서는 Claude Code가 이 저장소에서 작업할 때 따르는 지침이다.

## 프로젝트 목표

업무 일정·할 일·마감을 한눈에 관리하는 웹 달력. 개인 또는 소규모 팀이 브라우저에서 바로 쓴다. 핵심 가치는 **빠른 입력, 명확한 가독성, 군더더기 없는 화면**이다.

### 되는 것 (구현 완료)

- 월/주 보기 전환 — **기본은 주 보기.** 주 보기에서 일정 제목은 두 줄까지 표시한다.
- 일정 추가·수정·삭제 — 입력 항목은 아래 "일정 데이터 모델" 참조
- 일정 분류 — **회의, 출장, 기타.** 색이 아니라 라벨로 구분한다(칩 색은 아래 "참석자" 참조).
- 마감·우선순위 표시
- **변경 표시**: 일정이 새로 추가·수정되면 그 일정은 변경 시점부터 24시간 동안 화면에서 "변경됨" 표시가 난다 (아래 "변경 표시" 참조)
- **한국 공휴일 표시** — 2025–2027(음력·대체공휴일 포함), 일요일처럼 붉게 + 이름 병기 (`src/holidays.js`)
- **로그인·권한** — 읽기(보기)는 공개, 생성·수정·삭제는 로그인 필요 (아래 "백엔드" 참조)
- **사용자 계정 관리** — 관리자가 사용자 추가·삭제 (아래 "백엔드" 참조)
- 데이터 저장 — 백엔드(FastAPI+SQLite)에 저장 (아래 "백엔드" 참조)

### 나중에 (지금은 만들지 말 것)

- 반복 일정의 복잡한 규칙(RRULE 전체)
- 알림/푸시, 캘린더 외부 연동(구글 캘린더 등)
- 사용자 본인 비밀번호 변경 (계정 관리는 관리자의 추가·삭제까지만 구현)

새 기능을 추가하기 전에 위 범위인지 먼저 확인한다. 범위 밖이면 사용자에게 묻는다.

## 일정 데이터 모델

일정 하나는 다음 항목을 가진다. 입력 폼·표시·저장 모두 이 구조를 따른다.

1. **내용** (`title`) — 일정 제목/내용. 필수.
2. **시간** (`time`) — 날짜와 시간. 종일 일정도 허용. 필수.
3. **장소** (`location`) — 회의실/외부 장소 등. 선택.
4. **본부장 참석** (`headAttending`) — 본부장 참석 여부(일정 속성, boolean).
   - **일정 칩 색은 이 값이 정한다** — 참석이면 **강한 파랑 칩**, 미참석이면 **연한 회색 칩**. (분류는 색이 아니라 라벨로만 구분한다.)
   - 색만으로 구분하지 말 것: 파랑과 함께 굵은 제목·"본부장" 라벨 등 비-색 단서를 병행한다(색각 접근성).
   - 폼에서 `참석 / 미참석`을 고른다. 참석이면 참석자 명단 입력칸은 감춘다(본부장 참석 일정엔 명단 불필요).
   - **본부장을 참석자 이름 매칭으로 판단하지 말 것** — 예전엔 `Attendee.isHead` 였으나 이름 없이 체크만 하면 조용히 유실되는 결함이 있어 일정 속성으로 올렸다.
5. **참석자** (`attendees`) — 사람 목록(이름만). 미참석 일정에서 참석자를 남길 때 쓴다. 선택.
6. **담당부서** (`department`) — 주관/담당 부서. 선택. 주 보기에선 제목 아래 별도 줄로 작게 표시한다.

```
Event {
  id: string
  title: string            // 1. 내용 (필수)
  time: { start, end?, allDay: boolean }  // 2. 시간 (필수)
  location?: string        // 3. 장소
  headAttending: boolean   // 4. 본부장 참석 여부 → 칩 색을 정한다
  attendees: Attendee[]    // 5. 참석자(이름만, 미참석 일정용)
  department?: string      // 6. 담당부서
  category?: string        // 회의/출장/기타 (색이 아니라 라벨로 구분)
  priority?: 'high' | 'normal'  // 마감·우선순위 표시("우선" 태그)
  createdAt: timestamp
  updatedAt: timestamp     // 변경 표시 판단에 사용 (서버가 채움)
}

Attendee {
  name: string
}
```

## 변경 표시 (하루 동안)

일정이 **새로 추가되거나 수정되면**, 그 일정은 변경된 시점부터 **24시간 동안** 달력에서 눈에 띄게 표시된다.

- 판단 기준: 현재 시각 − `updatedAt` < 24시간이면 "변경됨" 상태.
- 표시는 디자인 클리셰(형광색 점, 무지개 배지)를 피하고, 이 달력의 절제된 팔레트 안에서 한 가지 단서로 통일한다 — 예: 일정 카드 왼쪽의 가는 강조선 + 작은 "변경" 라벨. 색만 쓰지 말고 라벨을 병행한다.
- 새로 추가된 것과 수정된 것을 구분할지는 선택(예: "신규" vs "수정" 라벨). 구분한다면 `createdAt == updatedAt` 여부로 판단한다.
- 24시간이 지나면 표시는 자동으로 사라진다. 화면이 떠 있는 동안에도 시간이 지나면 갱신되도록, 표시 여부를 렌더링 시점에 계산하거나 가벼운 타이머로 갱신한다(저장 데이터에 "변경됨" 상태를 박아두지 말 것 — `updatedAt`에서 매번 파생한다).

## 기술 스택

- 프론트: **바닐라 JS, 빌드 없음** (2026-06-25 확정). `index.html` importmap 으로 `date-fns` 를 로컬 동봉본으로 로드.
- 백엔드: **Python FastAPI + SQLite** (2026-07-09 승격). 아래 "백엔드" 참조.
- 날짜 처리: 직접 Date 연산 대신 `date-fns`(로컬 동봉 `vendor/date-fns.js`). 월 경계·시간대 버그를 직접 만들지 않는다.
- 저장: 모든 읽기/쓰기는 `storage` 모듈 하나로 격리한다(구현은 fetch). UI·달력 로직은 이 인터페이스만 안다.
- 상태/UI 로직과 렌더링을 분리한다. 달력 날짜 계산은 순수 함수(`src/calendar.js`)로 빼고 따로 테스트한다.
- 모듈 구성: `src/` = config·api·storage·auth·users·calendar(순수)·render·main·holidays. `test/` = node 테스트, `backend/` = FastAPI + 테스트.

## 백엔드

**2단계로 승격됨 (2026-07-09) — FastAPI + SQLite.** 사용자가 로그인·권한을 요청해 올렸다.
백엔드가 프론트 정적파일까지 함께 서빙하므로(동일 출처, CORS 없음) 서버는 하나다.

### 실행
- `python -m pip install -r backend/requirements.txt` (최초 1회)
- `npm run serve` (= `python -m uvicorn backend.main:app`, http://127.0.0.1:8000)

### API — `storage` 인터페이스를 그대로 REST로
- `GET /events`, `GET /events/:id` — **공개(읽기)**
- `POST /events`, `PATCH /events/:id`, `DELETE /events/:id` — **로그인 필요(쓰기)**
- `POST /auth/login`, `POST /auth/logout`, `GET /auth/me` — 세션 쿠키(Starlette SessionMiddleware)
- 사용자 관리(관리자 전용): `GET /users`, `POST /users`, `DELETE /users/:username` — 자기·마지막 관리자 삭제 방지, 중복 409
- `id`·`createdAt`·`updatedAt`은 **서버가** 채운다(클라 시계 불신 → 변경 표시가 서버 기준으로 일관되게). 시간은 UTC 저장, 표시만 로컬.

### 저장소
- SQLite. `events`(참석자는 JSON 컬럼), `users`(username, pw_hash, is_admin).
- 비밀번호 해시는 stdlib `hashlib.pbkdf2_hmac`(bcrypt/passlib 컴파일 의존 회피). 세션 서명은 itsdangerous.
- 동시성: `updatedAt` 기반 마지막-쓰기-우선(충돌 머지는 범위 밖).

### 프론트 `storage` (동기 유지)
- `list()`/`get()`은 인메모리 캐시에서 **동기** 반환(렌더는 동기 유지). `sync()`(async)가 서버에서 캐시를 채운다. `create`/`update`/`remove`는 async(성공 후 `sync()`).
- `storage` 인터페이스(이름·인자)는 유지한다. UI·달력 로직은 이 인터페이스만 안다.

### 지키기 (환경변수 — 비밀·URL 하드코딩 금지)
- 프론트: `src/config.js`의 `API_BASE`(기본 동일 출처).
- 서버: `CAL_SECRET`(세션 서명), `CAL_ADMIN_USER`/`CAL_ADMIN_PASS`(시드 관리자), `CAL_DB`(SQLite 경로).
- 미설정 시 dev 기본값(admin/admin 등) + stderr 경고. **운영에선 반드시 지정.**

## 디자인 원칙 — "AI가 만든 느낌"을 피하는 것이 최우선

이 프로젝트의 디자인은 **템플릿처럼 보이면 실패**다. 아래를 지킨다.

### 피해야 할 클리셰 (현재 AI 생성물이 몰리는 패턴)

- 크림색 배경(#F4F1EA 근처) + 고대비 세리프 + 테라코타 포인트
- 거의 검정 배경 + 형광 초록/주황 포인트 하나
- 신문 레이아웃: 머리카락처럼 얇은 선, border-radius 0, 빽빽한 칼럼
- 의미 없는 `01 / 02 / 03` 번호 마커, 무지개색 카테고리, 보라→파랑 그라데이션 버튼
- 모든 곳에 그림자와 둥근 모서리를 균일하게 바르기

위 세 가지는 "선택"이 아니라 "기본값"이라 어떤 주제에도 똑같이 나온다. 자유로운 축을 이런 기본값에 쓰지 않는다.

### 대신 할 것

- **주제에서 출발한다.** 이건 "업무용 달력"이다. 일하는 사람의 책상, 다이어리, 플래너, 포스트잇, 데스크 캘린더의 질감과 어휘에서 구체적 선택을 끌어온다. 장식이 아니라 일정을 _읽기 쉽게_ 만드는 방향이어야 한다.
- **색은 4~6개 명명된 hex로 한정**한다. 무지개를 피하고 절제된 팔레트 안에서 구분되게 한다. 색이 의미를 인코딩해야 한다 — 이 앱에선 **본부장 참석=강한 파랑, 그 외=연한 회색, 공휴일=경고색**. 분류는 색이 아니라 라벨로 구분한다.
- **타이포가 개성을 진다.** 디스플레이/본문 폰트를 의도적으로 짝짓는다. 날짜 숫자, 요일, 일정 제목의 위계를 또렷한 타입 스케일로 잡는다. 숫자(날짜)는 달력의 주인공이니 여기서 한 번 승부를 본다.
- **대담함은 한 곳에만.** 시그니처 요소(예: 오늘 날짜 처리, 마감 임박 표시, 주간 타임라인) 하나만 기억에 남게 만들고 나머지는 조용하고 규율 있게 유지한다.
- **모션은 절제.** 보기 전환이나 일정 추가 같은 한 순간만 매끄럽게. 곳곳에 흩뿌린 효과는 오히려 AI 느낌을 키운다. `prefers-reduced-motion`을 존중한다.

### 품질 기준 (말없이 충족)

- 모바일까지 반응형
- 키보드 포커스가 눈에 보일 것, 화살표 키로 날짜 이동 가능
- 색만으로 카테고리를 구분하지 말 것(라벨/아이콘 병행) — 색각 접근성
- 빈 화면("일정 없음")과 에러를 방향 제시의 기회로 쓴다. 사과하거나 모호하게 굴지 않는다.

### 카피(문구)도 디자인이다

- 사용자가 조작하는 말로 이름 붙인다: "저장", "삭제" — 시스템 내부 용어 금지.
- 능동태, 문장 부호 최소, 군더더기 없이. 버튼이 "게시"면 결과 토스트도 "게시됨".

## 작업 방식

### 프로젝트 특화 규칙

- 디자인을 코딩하기 전에, 위 원칙에 비춰 "이게 비슷한 요청에 늘 나오는 기본값인가?"를 자문한다. 그렇다면 바꾸고, 무엇을 왜 바꿨는지 한 줄로 남긴다.
- 날짜 경계(월말, 윤년, 주 시작 요일) 로직은 작성 후 반드시 몇 가지 케이스로 검증한다.
- CSS 선택자 특정성에 주의한다. 섹션 단위 클래스와 요소 단위 클래스의 padding/margin이 서로 상쇄되지 않게 한다.
- 한 번에 너무 많이 만들지 말고, 동작하는 작은 단위로 쌓는다.

### 일반 작업 원칙 (LLM 흔한 실수 방지)

이 원칙들은 속도보다 신중함에 무게를 둔다. 사소한 작업엔 판단해서 적용한다.

1. **코딩 전에 생각한다.** 가정하지 말고, 혼란을 숨기지 말고, 트레이드오프를 드러낸다. 구현 전에 가정을 명시하고, 불확실하면 묻는다. 해석이 여러 갈래면 임의로 하나 고르지 말고 제시한다. 더 단순한 방법이 있으면 말한다. 불분명하면 멈추고, 무엇이 불분명한지 짚고, 묻는다.
2. **단순함이 먼저다.** 문제를 푸는 최소 코드만. 요청하지 않은 기능·추상화·"유연성"·불가능한 시나리오용 에러 처리를 넣지 않는다. 200줄이 50줄로 될 것 같으면 다시 쓴다. "시니어 엔지니어가 과하다고 할까?" 자문해서 그렇다면 단순화한다.
3. **수술적 변경.** 꼭 필요한 곳만 건드린다. 인접 코드·주석·포맷을 "개선"하지 않고, 안 망가진 걸 리팩터하지 않고, 기존 스타일에 맞춘다. 내 변경이 만든 미사용 import·변수만 정리하고, 기존 데드코드는 발견하면 언급만 한다(요청 없이 지우지 않는다). 모든 변경 줄이 요청과 직접 연결되어야 한다.
4. **목표 주도 실행.** 성공 기준을 정의하고 검증될 때까지 반복한다. "검증 추가"→"잘못된 입력 테스트를 쓰고 통과시킨다", "버그 수정"→"버그를 재현하는 테스트를 쓰고 통과시킨다", "리팩터"→"전후로 테스트가 통과하는지 확인". 다단계 작업은 간단한 계획을 먼저 적는다(단계 → 검증 방법).
5. **닫는 콜론 금지 (한국어 출력).** 사용자가 한국어로 쓰면 출력도 한국어다. 다음 줄이 리스트·예시여도 문장을 `:` 로 끝내지 않는다. 문장 종결은 `.` `?` `!` 만. 콜론은 코드·키:값·라벨 안에서만 쓴다.
6. **새 소스 파일 첫 줄은 한국어 헤더 주석.** 파일의 역할을 한 줄로 적는다(예: `// 사용자 인증 상태를 관리하는 Context Provider`). `'use client'` 같은 지시어 바로 아래에 둔다. 설정 파일(`*.config.ts`, `package.json` 등)은 제외.
7. **계획 + 체크리스트 + 컨텍스트 노트.** 비-사소 작업은 세 가지를 먼저 만든다 — 계획(무엇을·왜), `checklist.md`(체크박스 작업, 진행하며 체크), `context-notes.md`(작업 중 내린 결정과 근거, 계속 추가). 사용자가 계획만 주고 바로 코딩하라 하면 멈추고 "체크리스트와 컨텍스트 노트를 먼저 만들까요?" 라고 묻는다.
8. **완료 표시 전에 테스트를 돌린다.** 코드를 건드렸으면 "끝", "완료" 라고 말하기 전에 프로젝트의 테스트를 실행한다. 통과하면 결과를 보고하고, 실패하면 고쳐서 다시 돌린다. 테스트 설정이 없으면 최소한 빌드·컴파일을 확인한다.
9. **시맨틱 커밋.** 한 논리적 변경이 끝나면 사용자가 요청하기 전에 커밋한다. "이 커밋을 한 문장으로 설명할 수 있나?" 그렇다면 커밋하고, 아니면 변경이 섞인 것이니 나눈다. 단, 1인 프로토타입·일회성 스크립트는 느슨하게 묶어도 된다(핵심은 의식이 아니라 되돌릴 수 있음).
10. **에러를 읽고, 추측하지 않는다.** 실패하면 전체 에러 메시지와 스택 트레이스를, 가정이 아니라 실제 로그 출력을 읽는다. 원인을 확인하기 전에 "흔한 수정"을 적용하지 않는다. 불분명하면 로그를 추가해 상태를 확인한 뒤 고친다.

## 커밋

- 한 커밋은 한 가지 일만. 메시지는 무엇을·왜를 짧게. (위 일반 원칙 9 참조)
- 동작하지 않는 코드는 커밋하지 않는다.
